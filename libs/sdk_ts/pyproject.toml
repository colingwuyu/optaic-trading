[project]
name = "sdk-ts-placeholder"
version = "0.0.0"
description = "Placeholder to satisfy Python workspace discovery."
requires-python = ">=3.11"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
